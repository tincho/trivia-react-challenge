import { render } from '@testing-library/react';
import Question from './Question';

describe('Question component', () => {
  test('render', async () => {
    // const { container } = render(<Question />);
  });
  test('should show category', async () => {
    // const { container } = render(<Question />);
  });

  test('should show question text', async () => {
    // const { container } = render(<Question />);
  });

  test('should show question n# of total', async () => {
    // const { container } = render(<Question />);
  });

  test('should present all answers in shuffled order', async () => {
    // const { container } = render(<Question />);
  });


  test('should present all answers as buttons', async () => {
    // const { container } = render(<Question />);
  });
  test('should present all answers in shuffled order', async () => {
    // const { container } = render(<Question />);
  });
  test('should pass answer to onAnswer on click its button', async () => {
    // const { container } = render(<Question />);
  });
});
